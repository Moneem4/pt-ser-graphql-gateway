# pt-ser-graphql-gateway
Required node >= 12

$ npm install

$ cp .env.example .env
# Running
$ npm run start

(for reload automatically https://nodemon.io)

$ npm install -g nodemon

$ npm run start-watch